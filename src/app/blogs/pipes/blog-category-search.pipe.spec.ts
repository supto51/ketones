import { BlogCategorySearchPipe } from './blog-category-search.pipe';

describe('BlogCategorySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BlogCategorySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
