import { TranslationInjectPipe } from './translation-inject.pipe';

describe('TranslationInjectPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslationInjectPipe();
    expect(pipe).toBeTruthy();
  });
});
