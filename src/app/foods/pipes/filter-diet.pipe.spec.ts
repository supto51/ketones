import { FilterDietPipe } from './filter-diet.pipe';

describe('FilterDietPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDietPipe();
    expect(pipe).toBeTruthy();
  });
});
