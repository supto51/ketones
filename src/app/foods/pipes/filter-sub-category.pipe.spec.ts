import { FilterSubCategoryPipe } from './filter-sub-category.pipe';

describe('FilterSubCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
