import { SearchBlogPipe } from './search-blog.pipe';

describe('SearchBlogPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBlogPipe();
    expect(pipe).toBeTruthy();
  });
});
